# Changelog

All notable changes to The Governance Framework will be documented in this file.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Added

- Initial repository scaffolding (Phase 1)
- `SKILL.md.template` matching Phase 0 anchored-section specification
- Artifact templates: `PROJECT-CONTEXT`, `DOMAIN-CONTEXT`, `DECISIONS`, `ROADMAP`, `ERROR-LOG`, `VENDOR-LOG`, `WAIVER-LOG`, `SCHEMA-HISTORY`, `SESSION-LOG`, `BASELINE-AUDIT`
- Security-conscious `.gitignore.template` (protective defaults)
- Plugin marketplace metadata (`.claude-plugin/plugin.json`, `marketplace.json`)
- Directory structure for skills, hooks (seven lifecycle events), stack-baselines, commands, and docs
- Initial `DECISIONS.md` capturing framework name, Path A scope, and Phase 0 architectural decisions
- Initial `ROADMAP.md` with 16-phase build plan and current status
- `DEC-2026-05-17-004`: authoritative source verification + no-downloads constraint during skill creation. Locks the discipline before Phase 2 research begins. Skills citing authoritative frameworks (OWASP, NIST, ISO, MITRE, RFCs) must verify rule-level citations against live sources at creation/refresh time, with research performed via Claude's web tools — no fetched content touches the developer filesystem except as synthesized output.
- `security-ai-research-integrity` added to Phase 8 scope (operationalizes DEC-004; fires when meta-skills fetch external content for skill generation or domain research).
- Phase 2 (CLAUDE.md expansion) shipped via four commits:
  - `c9ac67a` — §17 Citation Verification expanded (~15 → 46 lines) to reflect DEC-2026-05-17-004's six clauses: live verification at skill-creation time, rule-level citation precision, fetched content treated as untrusted input, no developer-machine downloads, paywalled sources cited by reference, comparative framework research separated from authoritative citation.
  - `92c9894` — §18 Hooks for Enforcement (71 lines) + §19 Token Efficiency (67 lines) + `DEC-2026-05-17-005` (hook architecture amendment to use Claude Code's actual PascalCase event taxonomy and add `.claude/git-hooks/` for git-layer enforcement) + hook directory restructure (`SessionStart`, `SessionEnd`, `PreToolUse`, `PostToolUse`, `SubagentStart`, `SubagentStop`, `FileChanged`, `ConfigChange`).
  - `3a48b0a` — §20 Agent Orchestration (100 lines: seven subagent roles + cost-aware dispatch by change tier + two-stage spec-then-quality review + LLM06 prevention strategies + MITRE ATLAS adversarial considerations) + §21 Self-Evolving Knowledge (96 lines: bounded evolution categories + confidence thresholds + LLM04 data-poisoning mitigation + four input streams).
  - Phase 2 closing commit (this one) — §22 Continual Improvement (67 lines: three improvement loops feeding each other — citation refresh, evolution proposals, telemetry analysis) + `templates/CLAUDE.md.template` (1091 lines, adopter-facing CLAUDE.md with header-level customization convention + inline ADOPTER-CUSTOMIZE markers at §7/§8/§9) + `docs/phase-2-plan.md` (Phase 2 implementation plan, committed for transparency per Decision 2 of the plan) + ROADMAP and CHANGELOG closeout.
- Phase 2 used the DEC-2026-05-17-004 research discipline throughout: authoritative source verification was performed via Claude's web tools on Anthropic infrastructure; no fetched content reached the developer filesystem except as synthesized citations and rules within markdown files. Sources verified during Phase 2: OWASP Top 10 for LLMs 2025 (LLM04, LLM06), NIST SP 800-218 v1.1 (SSDF), MITRE ATLAS v5.4.0 (February 2026), Anthropic Skill authoring best practices, Claude Code Hooks reference. Comparative-only source: Superpowers framework README (per DEC-004 Clause 6 — comparative research stays separate from authoritative citation).

### Changed

- **CLAUDE.md split into operational contract + extended architecture.** §15–§22 extracted from `CLAUDE.md` to new `docs/ARCHITECTURE.md` to keep per-session context under the 40k-char performance threshold. CLAUDE.md now holds §1–§14 (the operational contract that loads every session) and an "Extended architecture" pointer section. ARCHITECTURE.md holds the deeper architecture sections (mode scaling, empirical verification, citation discipline, hooks, token efficiency, agent orchestration, self-evolving knowledge, continual improvement). Three cross-references in §3, §4, §6 updated to point to `docs/ARCHITECTURE.md §15`. No content removed — just relocated. Result: CLAUDE.md from 75.9k → 37.8k chars (under threshold), ARCHITECTURE.md at 40.5k. Adopter `templates/CLAUDE.md.template` not yet split — deferred until adopter-distribution work in a later phase. (Commit `a630540`, 2026-05-19.)

### Phase 4 — Architectural Reconciliation (Pre-Implementation)

- **Repo restructured to plugin layout (housekeeping commit `49c8a82`).** Per `DEC-2026-05-19-007` and `DEC-2026-05-19-009`. Removed empty `.claude/skills/` placeholder and empty `.claude/hooks/<EventName>/` placeholder directories (8 PascalCase event dirs). Created plugin-root layout: `skills/` (with `.gitkeep` for skill content in Phase 4+), `agents/` (with `.gitkeep` for `tgf-orchestrator` and review subagent definitions in Phase 4/11), `hooks/hooks.json` (empty configuration object, ready for Phase 12), `hooks/scripts/` (with `.gitkeep` for hook script implementations in Phase 12). Kept `.claude/git-hooks/` (separate concern per DEC-005 + DEC-009). Kept `.claude/settings.local.json` (user's local settings, not framework). Plugin manifest `.claude-plugin/plugin.json` unchanged — was already correctly placed from Phase 1.
- **`docs/phase-4-plan.md` drafted (commit `50c1579`).** Phase 4 implementation plan covering: status against the architectural foundation (DEC-007/008/009), source verification per skill (NIST SSDF for CODE-QUALITY/CONTINUITY craft; OWASP Top 10:**2025** + ASVS 5.0 for SECURITY-CORE; ISO/IEC 27002 5.37 by-reference for CONTINUITY), per-skill mini-specs with target lengths and QC criteria (CODE-QUALITY, SECURITY-CORE, CONTINUITY each ≤300 lines SKILL.md + rules.md + anti-patterns.md reference files), implementation order, universal QC criteria (12 items including SKILL.md ≤300-line ceiling and ≤500-char description budget), five open Checkpoint 1 decisions (craft-vs-rule citation density, reference file scope, description style for always-on skills, review subagent scaffolds vs full, commit grouping with leans toward 6 focused commits). Plan committed for transparency per Phase 2 Decision 2. Estimated effort: ~18-20 hours focused work across 3-5 sessions.
- **Phase 4 Checkpoint 1 cleared (commit `847be3d`).** All five decisions resolved. Decision A (citation density) reframed as "cite at the source's natural granularity" — refinement of DEC-004 Clause 2's intent; craft rules cite at principle level where no rule-level mapping exists. Decision B (reference file scope) = ship `rules.md` + `anti-patterns.md` per skill from day one; defer `citations.md` split until rules.md exceeds ~400 lines. Decision C (always-on skill descriptions) = descriptions serve both adoption modes (orchestrator-active + skills-only); target ~300-500 chars. Decision D (review subagent definitions) = scaffolds in Phase 4 (skills: preload + memory: project + 5-10 line system prompt); full system prompts deferred to Phase 11. Decision E (commit grouping) = six focused commits (template update → 3 skills → agents → closeout). None of A–E warranted a new ADR; tactical decisions captured in plan file.
- **Phase 4 commit 1/6 — Template update (this commit).** Updated `templates/SKILL.md.template` from 270 → 212 lines per `DEC-2026-05-19-007` 300-line body budget. Frontmatter restructured to clearly separate Anthropic-native runtime fields (name, description, paths, disable-model-invocation, user-invocable, allowed-tools, model, effort, context, agent, hooks, shell) from TGF-extension metadata (applies-when sub-fields beyond paths, disqualifying-when, sources, last-generated, refresh-recommended, self-evolution) with comment dividers explaining that TGF-extension fields are documentation for Phase 11 meta-skills, not Claude Code runtime gates. Sections refactored: §5 Numbered Rules (verbose inline) → §5 Rule Summaries (1-line + citation per rule, pointing to rules.md); §6 Anti-Patterns + §7 Canonical Patterns (separate inline sections) → §6 Anti-Pattern Summaries (1-line per anti-pattern, pointing to anti-patterns.md where pairs live together). Section count 10 → 9. Added §2 citation granularity guidance per Checkpoint 1 Decision A. Added header note explaining reference-file pattern + 300-line budget. New templates created: `templates/rules.md.template` (80 lines — full rule structure with statement + citation + plain-language impact + extended discussion + related anti-patterns); `templates/anti-patterns.md.template` (137 lines — AP-N + CP-N pairs with code examples, pairing discipline noted). Establishes the convention all Phase 4–10 skills inherit.

- **`DEC-2026-05-19-007` added: TGF architecture as plugin with orchestrator agent.** Phase 4 deep research surfaced that Anthropic's Agent Skills spec is purely description-driven — there is no native `always-on: true` field. Three viable always-on mechanisms were considered; selected approach uses the Claude Code `--agent` flag with a custom orchestrator agent (`tgf-orchestrator`) that preloads CODE-QUALITY, SECURITY-CORE, and CONTINUITY via the Anthropic-native `skills:` frontmatter field. Plugin distribution is primary; standalone `.claude/` usage remains secondary. Review subagents (code-reviewer, security-auditor, red-team, holistic-reviewer) defined in `agents/` with per-subagent skill preloads and `memory: project` for self-evolution data accumulation. Replaces the SessionStart-hook injection approach proposed earlier — Anthropic provides the mechanism natively for agent contexts. `DEC-2026-05-17-003` Clause 1 is amended in effect: TGF-extension frontmatter fields (`sources`, `applies-when` sub-fields beyond `paths`, etc.) become TGF-internal metadata for Phase 11 meta-skill consumption, not Claude Code runtime fields. `.tgf/evolution/observations/` directory specified in `DEC-2026-05-17-003` Clause 4 is replaced by Anthropic's native `.claude/agent-memory/` mechanism; the `proposals/{pending,accepted,rejected}/` workflow stays TGF-internal.
- **`DEC-2026-05-19-008` added: Principled skill catalog consolidation.** Two consolidations: architectural cluster (5 skills `security-defense-in-depth` + `security-zero-trust` + `security-least-privilege` + `security-assumed-breach` → 1 `security-architectural-principles`; `security-secure-architecture` stays separate); CIA triad (3 skills → 1 `security-cia-triad`). Net reduction: ~80 → ~75 skills. Clusters NOT consolidated: IAM (4 skills stay split — highest-frequency domain), data layer (3 stay split), threat management (3 stay split). Establishes the reference-file pattern as standard: SKILL.md body ≤300 lines; verbose content (full rules, anti-patterns with code, citation tables) moves to `rules.md`, `anti-patterns.md`, `citations.md` reference files loaded on demand per Anthropic's spec. Solves citation density without dilution.
- **`DEC-2026-05-19-009` added: Hook physical layout amendment.** Amends `DEC-2026-05-17-005` for the physical layout (event taxonomy unchanged). TGF's distributed form uses plugin-native `hooks/hooks.json` (JSON configuration object) at the plugin root with hook scripts in `hooks/scripts/`. The PascalCase directory convention from DEC-005 remains valid for standalone `.claude/hooks/` usage but is not used in plugin distribution. Pre-Phase-4 housekeeping commit removes the empty `.claude/hooks/<EventName>/` placeholder directories.
- **Phase 4 research source verification (2026-05-19, per `DEC-2026-05-17-004`):** Verified via Claude's web tools: Anthropic Agent Skills authoring (frontmatter spec, ~1024 char description cap, ~1% skill-listing-budget context fraction, 500-line SKILL.md body recommendation, reference file pattern, skill content lifecycle with 25k token re-attachment budget after compaction); Claude Code Subagents (full frontmatter field list including `skills:` preload, `memory:`, `mcpServers:`, `hooks:`, `initialPrompt:`, `effort:`, `isolation:`, `--agent` flag for main-session use, subagents cannot spawn subagents constraint); Claude Code Plugins (manifest schema, plugin layout `skills/`+`agents/`+`hooks/hooks.json` at plugin root, `settings.json` with `"agent"` activation, background monitor capability, plugin namespace `/plugin-name:skill-name`); OWASP Top 10:2025 (released; SECURITY-CORE will cite 2025 not 2021 — new categories A03 Software Supply Chain Failures, A10 Mishandling of Exceptional Conditions; Security Misconfiguration moved from #5 to #2); agentskills.io open standard (cross-tool portability across ~40 AI tools including Cursor, OpenAI Codex, Gemini CLI, VS Code, GitHub Copilot; standard is description-driven only — TGF-specific features like `skills:` preload are Claude Code extensions).

### Phase 4 — Implementation Complete (2026-05-20)

- **Pre-publish housekeeping (commit `b302eb6`).** README.md rewritten in author's first-person narrative voice (anecdote opener; "So I built one." punch; concrete numbers including ~75 skill count per `DEC-2026-05-19-008`; "drop an issue" close-out invitation). `.claude-plugin/plugin.json` homepage and repository URLs corrected from `alt313/tgf` to `alexlearnstech313/tgf` to match actual GitHub username. Two-file commit landing as `b302eb6`.
- **First public push to `github.com/alexlearnstech313/tgf`.** Repo created via GitHub web dashboard; pushed via fine-grained Personal Access Token (Contents: Read and write scope on `tgf` only). 123 objects, 186 KiB. Plugin manifest URLs now valid; README renders in voice. `.sessions/` and other operational state correctly gitignored — confirmed in browser file tree post-push.
- **Phase 4 commit 2/6 (commit `1157e8c`) — `skills/code-quality/`.** First always-on skill content. 845 lines across three files: SKILL.md (236 lines, 9 anchored sections, description ~430 chars), rules.md (142 lines, 6 rules covering type safety at boundaries / explicit error handling / names describe intent / comment WHY-not-WHAT / scale-aware defaults / solo-maintainability), anti-patterns.md (461 lines, 8 AP/CP pairs with TypeScript + Python code examples). Citations: NIST SSDF v1.1 PW.4/PW.5/PW.7 at practice level + Anthropic Skills authoring (current) + TGF synthesis for craft rules without rule-level external mapping (per Phase 4 Checkpoint 1 Decision A). Four-pass review caught two fixes: AP-2 CP added explicit `ChargeResult` dataclass; AP-4 CP switched from sync `bcrypt.hashSync` to async `await bcrypt.hash` (avoids contradicting Rule 5.5 in the canonical example).
- **`DEC-2026-05-20-010` added (commit `7c81170`): Disable security-guidance plugin hook for TGF.** Sub-workflow during Phase 4 commit 3/6 surfaced that the Anthropic-published `security-guidance@claude-plugins-official` plugin hook scans Write/Edit content for substrings including `exec(`, `os.system`, `pickle`, `eval(`, `new Function`, `dangerouslySetInnerHTML`, `document.write`, `.innerHTML =`, GitHub Actions workflow paths — and exits with code 2 to block on first match. This is hostile to TGF's nature by design (TGF is itself a security framework whose content explicitly catalogs these patterns as anti-patterns). Phase 6 (11 security skills) + Phase 7 (22 more) would compound the friction. The hook author built a documented kill switch (`ENABLE_SECURITY_REMINDER` env var); using it via gitignored `.claude/settings.local.json` is the cleanest mechanism. ADR rejected 4 alternatives (disable plugin globally; modify plugin source; retry every block; restructure content). User pushed for full workflow rigor on the decision: Stage 1 research established hook scope (5 trigger rules + path check; ~9 substring categories); Stage 2 scope (single file change + ADR); Stage 3 governance applied SECURITY-CORE principles to confirm defense substitution by TGF's four-pass review is adequate; Stage 5 four-pass review clean; Stage 6 commit `7c81170`. Adopter projects unaffected — TGF's plugin manifest doesn't declare `security-guidance` as a dependency. .claude/settings.local.json change is local-only (gitignored); only DECISIONS.md changes in the commit.
- **Phase 4 commit 3/6 (commit `b49715d`) — `skills/security-core/`.** Second always-on skill. 1156 lines across three files: SKILL.md (270 lines, 9 sections, description ~490 chars enumerating hard-refusal list), rules.md (207 lines, 7 rules covering input validation / authorization / established cryptography / secrets / TLS verification / output encoding / security logging), anti-patterns.md (679 lines, 9 AP/CP pairs). The `CLAUDE.md` §5 hard-refusal list (7 items) maps 1:1 to APs: AP-1 hardcoded credentials, AP-2 custom crypto, AP-9 disabled authentication middleware, AP-3 disabled TLS, AP-4 MD5/SHA-1 for security, AP-5 logging sensitive data, AP-7 authorization by obscurity. AP-6 SQL injection + AP-8 shell from user input cover the most common injection patterns. Stage 1 spot-check (2026-05-20) surfaced two important corrections to plan-stage citations: (1) OWASP ASVS 5.0.0 chapter numbering completely reorganized from 4.x (released 2025-05-30 at Global AppSec EU Barcelona) — `phase-4-plan.md` §3 references reflected old 4.x V-numbers (V1 Architecture, V2 Authentication, V3 Session, V4 Access Control, V8 Data Protection, V13 API); SECURITY-CORE uses actual 5.0 numbering (V1 Encoding/Sanitization, V2 Validation/Business Logic, V4 API/Web Service, V6 Authentication, V7 Session Management, V8 Authorization, V11 Cryptography, V12 Secure Communication, V14 Data Protection, V16 Security Logging/Error Handling); (2) OWASP Top 10:2025 confirmed released with new categories A03 Software Supply Chain Failures, A10 Mishandling of Exceptional Conditions; Security Misconfiguration moved from #5 (2021) to #2 (2025). Stage 5 four-pass review caught a gap: hard-refusal item #3 ("Disabled authentication on auth-handling endpoints") was initially uncovered — AP-9 added to close, restoring 1:1 mapping between hard-refusal list and APs.
- **Phase 4 commit 4/6 (commit `511f585`) — `skills/continuity/`.** Third and final always-on skill. 1080 lines across three files: SKILL.md (231 lines, 9 sections), rules.md (176 lines, 6 rules covering session-close log discipline / ADRs / three-log routing / ROADMAP reality / information disclosure / capture-WHY-not-WHAT), anti-patterns.md (673 lines, 8 AP/CP pairs with concrete markdown excerpts demonstrating the discipline rather than code). Citations per Phase 4 Checkpoint 1 Decision A: NIST SSDF v1.1 PO.5 at practice level + ISO/IEC 27002:2022 control 5.37 by reference (paywalled) + ADR methodology (Michael Nygard 2011) at paper level (stable since); the rest acknowledged as TGF synthesis. Stage 1 step caught a reflexive habit — initially queued a fetch of adr.github.io to "verify ADR methodology" before realizing TGF's own committed DECISIONS.md (10 ADRs at time of skill creation) and DECISIONS.md.template are the operational ground truth for the format; upstream fetch was circular. Skipped without harm. Meta-observation: CONTINUITY governs the artifacts CONTINUITY itself produces (session logs, ADRs, the very commit message); Rule 5.6 (capture WHY) applied recursively to the skill's own extended discussions.
- **Phase 4 commit 5/6 (commit `d4abbb0`) — `agents/` scaffolds + plugin-root `settings.json`.** Five agent definitions (122 lines total) plus one-line `settings.json`. Per Phase 4 Checkpoint 1 Decision D: scaffolds with `skills:` preload + `memory: project` + 5-10 line system prompts; full system-prompt content deferred to Phase 11 when the orchestration meta-skill builds out review-pass semantics. `agents/tgf-orchestrator.md` (26 lines): main-session agent preloading all three always-on skills (`tgf:code-quality` + `tgf:security-core` + `tgf:continuity`); becomes the default when TGF is installed as a plugin via plugin-root `settings.json`. `agents/code-reviewer.md` (21 lines): Phase 1 — preloads tgf:code-quality. `agents/security-auditor.md` (24 lines): Phase 2 — preloads tgf:security-core; hard-refusal-list findings flagged as Critical severity. `agents/red-team.md` (23 lines): Phase 3 — preloads tgf:security-core (rules as floor + APs as attack-scenario seed). `agents/holistic-reviewer.md` (25 lines): Phase 4 — preloads tgf:continuity (primary) + tgf:code-quality (Rule 5.6 solo-maintainability check). Subagent skill mappings consistent end-to-end with each skill's §9 Subagent Context section. `settings.json` placement (`{"agent": "tgf-orchestrator"}` at plugin repo root per `phase-4-plan.md` §4 step 6) — exact activation mechanism for plugin-distributed default-agent activation not unambiguously confirmed at session time; if Phase 11 verifies a different mechanism is needed (e.g., a `defaults` block under `.claude-plugin/` or activation field in `plugin.json`), the agent definitions remain correct regardless and the activation file relocates. `agents/.gitkeep` removed (real content now present). Token cost per `DEC-2026-05-19-007` + `docs/ARCHITECTURE.md` §19: preloading three always-on skills costs ~20k tokens at session start; the 300-line SKILL.md ceiling (DEC-007) makes this cost predictable and bounded; reference files (rules.md, anti-patterns.md) load on demand during review, not at session start.
- **Phase 4 commit 6/6 (this commit) — Closeout.** Updates `CLAUDE.md` §6 to reflect the orchestrator-preload activation mechanism (per DEC-007) instead of language that implied Anthropic-native "always-on" was a thing; mentions `tgf-orchestrator` agent + `skills:` field + plugin-root `settings.json`. Updates `CLAUDE.md` §9 with: `skills/` path correction (per `DEC-2026-05-19-009` plugin layout — was `.claude/skills/`); DEC-008 consolidations now reflected in the universal-security-skills enumeration (CIA triad: 3 → 1 `security-cia-triad`; architectural cluster: 4 → 1 `security-architectural-principles` while `security-secure-architecture` stays separate); `security-ai-research-integrity` added to AI-specific list (was missing despite being in ROADMAP Phase 8); `framework-health/` added to meta-skills (was missing despite being in ROADMAP Phase 11); total catalog target ~75 skills noted explicitly. Updates `ROADMAP.md`: Phase 4 status → ✅ Complete; M4 moved from Active to Completed with date 2026-05-20 and full commit-by-commit notes; M5 (Phase 5 — activity skills) added to Active Milestones; Current Focus pointer rolls to Phase 5; Last updated → 2026-05-20. Updates `CHANGELOG.md` with this section. Session log entry at `.sessions/2026-05-20-session-01-phase-4-implementation.md` captures the session's full scope (first public push + voice rewrite + three always-on skills + the security-hook sub-workflow + DEC-010 + agent scaffolds), the two Stage 1 plan-adjustments worth remembering (ASVS 5.0 numbering reorganization; reflexive over-fetch instinct), three findings caught in four-pass reviews (AP-2 ChargeResult, AP-4 bcrypt async, AP-9 disabled-auth gap), and the meta-observation that CONTINUITY governs its own artifacts.

### Phase 5 — Implementation Complete (2026-05-20)

- **Phase 5 plan drafted (commit `4f3a5f8`).** `docs/phase-5-plan.md` covering six activity skills (PROJECT-MANAGEMENT, DISCOVERY, TESTING, DEBUGGING, DISAGREEMENT, DESIGN) with per-skill mini-specs, sources verification queued for Stage 1 of each skill, implementation order (DISCOVERY → PROJECT-MANAGEMENT → DESIGN → TESTING → DEBUGGING → DISAGREEMENT → closeout), universal QC criteria, and five open Checkpoint 1 decisions (DESIGN scope, TESTING pyramid-vs-trophy, DISAGREEMENT vs CLAUDE.md §5 boundary, DEBUGGING subagent role, commit grouping).
- **Phase 5 Checkpoint 1 cleared (commit `2247b2a`).** All five decisions resolved without new ADRs — tactical to Phase 5. Decision A: DESIGN universal principles + cross-refs to domain skills for depth. Decision B: TESTING presents both pyramid AND trophy with when-each-fits guidance. Decision C: DISAGREEMENT references §5 for severity definitions + adds operational layer. Decision D: defer dedicated `debugger` subagent to Phase 11 (consistent with Phase 4 Decision D). Decision E: seven focused commits matching Phase 4 cadence (later amended to 8 per Decision F).
- **Phase 5 commit 1/8 (commit `563da7e`) — `skills/discovery/`.** First activity skill. 811 lines across SKILL.md (208 lines, 9 anchored sections) + rules.md (123 lines, 5 rules — narrow before answering, branching-tree questioning, Five Whys for root cause, surface assumptions explicitly, stop at scope-operational) + anti-patterns.md (480 lines, 8 AP/CP pairs with example dialogues). Sources: Sakichi Toyoda Five Whys (~1950s, stable), IIBA BABOK by reference (paywalled), NIST SSDF PO.1.
- **Phase 5 commit 2/8 (commit `bce6bfa`) — `skills/project-management/`.** Second activity skill. 934 lines. 6 rules covering intent-first / greenfield-mode / brownfield-mode / MVP-serves-users / constraint-aware-decomposition / surface-dependencies. 8 AP/CP pairs including AP-4 brownfield-ignoring-existing-reality with concrete strangler-fig migration example. Cross-skill integration with DISCOVERY (intent surfacing via branching-tree), CODE-QUALITY Rule 5.6 (solo-maintainability), CONTINUITY Rule 5.4 (ROADMAP reality), SECURITY-CORE (trust boundaries), forward-reference to BASELINE-AUDIT (Phase 11). Sources: NIST SSDF PO.1/PO.2/PO.3, Agile Manifesto (stable since 2001), PMBOK 7th Ed by reference (paywalled), Eric Ries Lean Startup as comparative source.
- **Phase 5 commit 3/8 (commit `2f8bc2e`) — `skills/design/`.** Third activity skill. 1015 lines. 6 rules: start-from-constraints, negative-constraints-first-class, simplest-design-wins, change-accommodation-not-anticipation, AI-patterns-are-hypotheses, accessibility-designed-in. 8 AP/CP pairs. Per Decision A: universal design principles + cross-references to domain skills for depth. Sources verified at Stage 1: Anthropic "Building Effective Agents" (anthropic.com/engineering/building-effective-agents, published 2024-12-19; three principles — Simplicity, Transparency, Tool Documentation & Testing; six workflow patterns); WCAG 2.2 (w3.org/WAI/standards-guidelines/wcag/, W3C Recommendation 2023-10-05, updated 2024-12-12; four POUR principles — Perceivable, Operable, Understandable, Robust).
- **Phase 5 plan amendment (commit `45fc38d`) — Decision F: add UI-CRAFT.** Mid-phase scope amendment surfaced during commit 3/8 (DESIGN). User identified that DESIGN's "decision discipline" scope doesn't cover **design craft** — the difference between polished intentional UI and recognizable "AI slop" (default Tailwind everything, color sprawl, monotone typography, default spacing, indiscriminate motion, forgotten states, maximalist accumulation). Decision A's framing assumed depth lived in domain skills (testable, secure, schema, API) — but design craft has no obvious domain home; it earns its own skill. Decision F captured: (i) new sibling skill UI-CRAFT alongside DESIGN — CHOSEN; alternatives rejected (expand DESIGN, defer to Phase 9, Phase 13 SKILL-FORGE bridge). Phase 5 grew 6→7 skills, commit grouping 7→8. Plan-file amended in place per Phase 4 precedent; no new ADR — tactical to Phase 5.
- **Phase 5 commit 4/8 (commit `404840e`) — `skills/ui-craft/`.** UI/UX craft anti-AI-slop skill. 1220 lines (largest activity skill). 7 rules covering design-system thinking, restrained color palettes, typographic hierarchy, spacing rhythm, motion as communication, designed states across the board, restraint earns trust. 8 AP/CP pairs with code examples + high-craft reference site studies (Stripe, Vercel, Linear, Monogram, Kraken per DEC-2026-05-17-004 Clause 6 as comparative pattern references — they inform CP examples without being authoritative citations). Authoritative grounding: Apple Human Interface Guidelines + Material Design 3 (both SPA-rendered and unfetchable via WebFetch — cited by reference at foundation level per same pattern as PMBOK/BABOK/ISTQB paywalled-source pattern; the sources are real and publicly published; URL stability high), Nielsen 10 Usability Heuristics, WCAG 2.2 cross-reference from DESIGN. Anti-AI-slop discipline is TGF synthesis honestly acknowledged — observable 2024-2026 phenomenon not yet codified in any single authoritative standard.
- **Phase 5 commit 5/8 (commit `db4d8bc`) — `skills/testing/`.** Test strategy + coverage discipline + security testing + accessibility testing + AI tautological-test audit. 1260 lines. 7 rules per Decision B (pyramid AND trophy both presented with when-each-fits guidance for TGF's multi-domain adopters spanning backend / frontend / library code). 8 AP/CP pairs including AP-1 tautological-tests + Rule 5.7 behavioral-audit (most-common AI test-generation failure mode), AP-7 security-relevant-change-without-WSTG-aligned-testing, AP-8 axe-only-accessibility-testing (catches ~30-40%; manual covers the rest). Stage 1: OWASP WSTG v4.2 confirmed current (v5.0 in development); ISTQB Foundation Level Syllabus v4.0 cited by reference (istqb.org returned 403 to WebFetch); NIST SSDF PW.7/PW.8 + WCAG 2.2 already verified.
- **Phase 5 commit 6/8 (commit `c34417e`) — `skills/debugging/`.** Systematic debugging discipline. 1237 lines. 7 rules — 5 mapping to specific Agans 9 rules (5.1→#2 Make it fail; 5.2→#3 Quit thinking and look; 5.3→#5 Change one thing at a time; 5.5→#6 Keep an audit trail; 5.6→#8 Get a fresh view), plus Rule 5.4 (Five Whys, Toyoda) and Rule 5.7 (AI debug outputs are hypotheses not conclusions — the most-dangerous AI debugging failure mode: plausible-sounding fabricated root causes that derail entire investigations). 8 AP/CP pairs. §8 maps cleanly to docs/WORKFLOW.md §7 debugging variant. Per Decision D: DEBUGGING activates at orchestrator level; no dedicated `debugger` subagent in Phase 5 (deferred to Phase 11). Sources all stable methodology — no live fetches needed.
- **Phase 5 commit 7/8 (commit `489a491`) — `skills/disagreement/`.** Operationalizes CLAUDE.md §5 (Authority Structure) severity gradient. 1144 lines. Per Decision C: references §5 for severity definitions and adds operational layer (contextual triggers in §3, worked examples per severity tier in Rule 5.2, anti-patterns showing gradient in practice). 7 rules including Rule 5.5 (hard-refusal items require explicit acknowledgment — framework executes user decision AFTER acknowledgment, not refusal), Rule 5.6 (waiver protocol cross-referencing CONTINUITY Rule 5.3 with required revisit conditions), Rule 5.7 (defend against AI sycophancy — most-important AI-specific concern this skill addresses). 8 AP/CP pairs including AP-5 (Stripe live key hardcoded for "quick demo" → ~$15k fraud) + AP-6 (auth disabled for "testing" → permanent production exposure). Cross-skill web at maximum coherence here — references SECURITY-CORE hard-refusal items + CONTINUITY waiver/ADR protocols + the four review subagents.
- **Phase 5 commit 8/8 (this commit) — Closeout.** Updates `CLAUDE.md` §9 Activity skills list with all 7 skills marked ✅ Phase 5 (one-line descriptions); UI-CRAFT noted as Decision-F mid-phase addition. Updates `ROADMAP.md`: Phase 5 status → ✅ Complete (note: 7 skills, grew from 6 mid-phase per Decision F); M5 moved from Active to Completed with date 2026-05-20 and full commit-by-commit notes including the Decision F amendment; M6 (Phase 6 — foundation security skills, 11 skills) added to Active Milestones; Current Focus pointer rolls to Phase 6; Last updated → 2026-05-20. Updates `CHANGELOG.md` with this section. Session log entry at `.sessions/2026-05-20-session-02-phase-5-implementation.md` (session 02 on same date as Phase 4 closeout) captures the full Phase 5 scope including the Decision F mid-phase amendment, Stage 1 plan-adjustments (Apple HIG / Material 3 / ISTQB unfetchable via WebFetch — cited by reference; OWASP WSTG v4.2 verified current), cross-skill composition observations (the framework is composing visibly — DISAGREEMENT references SECURITY-CORE + CONTINUITY; UI-CRAFT cross-refs DESIGN; TESTING cross-refs SECURITY-CORE + DESIGN + UI-CRAFT; DEBUGGING cross-refs CONTINUITY + DISCOVERY + TESTING), and one conversational moment worth preserving (user catching the DESIGN scope gap and surfacing the UI-CRAFT need — concrete example of Rule 5.3 DISCOVERY-style scope refinement at phase-implementation time, validating both the workflow and the user's instinct for what TGF's mission requires).

Phase 5 totals: 7 activity skills shipped, 7,621 lines across 21 files. Plus plan + Checkpoint + amendment + closeout = 10 commits total. Combined with Phase 4's 3,081 lines of always-on skills + 122 lines of agents, TGF now ships 10 substantive skills (3 always-on + 7 activity) totaling 10,824 lines of skill content, plus 5 agent scaffolds, plus the foundational architecture (CLAUDE.md + ARCHITECTURE.md + WORKFLOW.md). Next: Phase 6 (Foundation Security Skills — 11 skills) begins the substantial security-skill push (Phases 6/7/8 = 11+22+9 = 42 security-and-AI-security skills — the largest remaining workload).



- `docs/phase-3-plan.md` drafted (Phase 3 implementation plan for `docs/WORKFLOW.md`). Specifies per-section mini-specs, per-section QC criteria, implementation order, sources (primarily internal synthesis of CLAUDE.md §3 + ARCHITECTURE.md §15/§18/§19/§20 + DECISIONS.md DEC-003/DEC-005), and five open decisions for Checkpoint 1 (TGF context injection mechanism for hooks, subagent schema format, hook section organization, worked-example domain choice, commit grouping). Surfaced one design question requiring resolution: how TGF injects `project_mode` and `change_tier` into Claude Code hooks (Claude Code's hook stdin JSON doesn't carry TGF concepts). Per Phase 2 Decision 2, the plan is committed for transparency before implementation. Estimated implementation effort: one focused session (~4–5 hours active work).
- **Checkpoint 1 cleared (2026-05-19).** All five decisions resolved. Decision A (TGF context injection mechanism) captured as `DEC-2026-05-19-006` (TGF session state architecture — file-based, session-keyed at `.tgf/state/sessions/{session_id}.json`); Decisions B–E (subagent schema format = TypeScript-style interface notation; hook contract organization = per-event with purpose cross-reference table; worked-example domain = generic; commit grouping = two commits split structure/content) captured in `docs/phase-3-plan.md` §7. WORKFLOW.md implementation cleared to begin next session.
- `DEC-2026-05-19-006` added to `DECISIONS.md`: TGF session state architecture. Specifies session-keyed JSON file at `.tgf/state/sessions/{session_id}.json` for `project_mode`, `change_tier`, `current_stage`, and forward-compatible future per-session state. Orchestrator writes; hooks and meta-skills read. Closes the gap left by DEC-003 Clause 2 (which invented TGF concepts into hook stdin JSON) and DEC-005 (which fixed event names but didn't address TGF-context injection). Constrains Phase 11 (orchestration meta-skill) and Phase 12 (hook scripts). Approved subject to Phase 3 Step 1 spot-check of the Claude Code Hooks reference.
- **Phase 3 Step 1 spot-check completed (2026-05-19).** Re-verified Claude Code Hooks reference. Confirmed DEC-006 is correct — no built-in mechanism exists for SessionStart→downstream-hook state sharing; file-based session-keyed state is the only documented path. Surfaced three adjacent findings: (1) richer `hookSpecificOutput.PreToolUse.permissionDecision` schema (`"allow" | "deny" | "ask" | "defer"`) provides cleaner block UX than exit 2 + stderr; (2) `CLAUDE_ENV_FILE` mechanism could complement DEC-006 by exposing TGF context to Bash tool invocations; (3) richer hook stdin JSON fields (`transcript_path`, `effort`, agent context) and additional events (`Setup`, `InstructionsLoaded`, `UserPromptExpansion`, `Elicitation`) exist beyond DEC-005's enumeration. ARCHITECTURE.md §18 remains accurate (its "at minimum" hedge holds); WORKFLOW.md §6 uses the richer schema.
- **Phase 3 WORKFLOW.md commit 1 of 2.** Lands §1 (Purpose and Scope), §2 (Conceptual Model with stage state machine, transitions, termination conditions, handoff contract table), §4 (Subagent Output Schemas — TypeScript-style interfaces for all 7 roles per Decision B, with worked example per role per per-section QC), §5 (Tier and Mode Scaling Tables with composition rule and 3 worked composition examples), §6 (Hook Integration Contracts — "Hooks by purpose" cross-reference table per Decision C, per-event contracts for all 8 used events, TGF context injection mechanism with session_id validation hardening, richer `hookSpecificOutput` schema with `"defer"` semantics flagged for Phase 12 re-verification, three universal hooks with `WAIVER-LOG` override semantics), §9 (Reference).
- **Phase 3 WORKFLOW.md commit 2 of 2 (this commit) — Phase 3 ✅ Complete.** Lands the substantive bulk: §3 Per-Stage Specifications (~195 lines, six stages each specifying inputs, operations with mode/tier conditionals, outputs, skill activation points, hook integration points, subagent dispatch points, and failure modes — including reduced-subagent re-review on rework loops surfaced in Stage 5 QC), §7 Debugging Variant (~32 lines, one-to-one mapping table of building→debugging stages with termination criteria for fixed/worked-around/cannot-reproduce/not-a-bug outcomes), §8 Worked Examples (~80 lines, three end-to-end traces: Trivial typo fix exercising minimal infrastructure, Medium Next.js auth middleware refactor with 5 findings across all severities, Large Stripe billing integration with decomposed implementation, 14 findings across all four reviewer roles, multi-commit landing, full artifact updates). Total WORKFLOW.md: 911 lines.
- **Workflow discipline applied throughout Phase 3 construction (dogfooded).** Stage 1 Research included spot-check of Claude Code Hooks reference (validated DEC-006, surfaced richer schema). Stage 2 Scope = phase-3-plan.md. Stage 3 Plan with Governance applied always-on skill principles (CODE-QUALITY for navigability, SECURITY-CORE for deny-by-default in §6, CONTINUITY for the spec's continuity role). Stage 4 Implement split across two commits per Decision E. Stage 5 Four-Pass Review (with orchestrator playing all four reviewer roles since skills don't exist yet) surfaced findings on both commits: commit 1 (missing worked examples for 5 roles → added, session_id path-traversal risk → validation added, "defer" permissionDecision ambiguity → flagged); commit 2 (rework re-review depth unspecified → §3 Stage 5 failure modes augmented). Stage 6 Commit on each chunk. Meta-circular but the discipline held.
