# Changelog

All notable changes to The Governance Framework will be documented in this file.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Added

- Initial repository scaffolding (Phase 1)
- `SKILL.md.template` matching Phase 0 anchored-section specification
- Artifact templates: `PROJECT-CONTEXT`, `DOMAIN-CONTEXT`, `DECISIONS`, `ROADMAP`, `ERROR-LOG`, `VENDOR-LOG`, `WAIVER-LOG`, `SCHEMA-HISTORY`, `SESSION-LOG`, `BASELINE-AUDIT`
- Security-conscious `.gitignore.template` (protective defaults)
- Plugin marketplace metadata (`.claude-plugin/plugin.json`, `marketplace.json`)
- Directory structure for skills, hooks (seven lifecycle events), stack-baselines, commands, and docs
- Initial `DECISIONS.md` capturing framework name, Path A scope, and Phase 0 architectural decisions
- Initial `ROADMAP.md` with 16-phase build plan and current status
- `DEC-2026-05-17-004`: authoritative source verification + no-downloads constraint during skill creation. Locks the discipline before Phase 2 research begins. Skills citing authoritative frameworks (OWASP, NIST, ISO, MITRE, RFCs) must verify rule-level citations against live sources at creation/refresh time, with research performed via Claude's web tools — no fetched content touches the developer filesystem except as synthesized output.
- `security-ai-research-integrity` added to Phase 8 scope (operationalizes DEC-004; fires when meta-skills fetch external content for skill generation or domain research).
- Phase 2 (CLAUDE.md expansion) shipped via four commits:
  - `c9ac67a` — §17 Citation Verification expanded (~15 → 46 lines) to reflect DEC-2026-05-17-004's six clauses: live verification at skill-creation time, rule-level citation precision, fetched content treated as untrusted input, no developer-machine downloads, paywalled sources cited by reference, comparative framework research separated from authoritative citation.
  - `92c9894` — §18 Hooks for Enforcement (71 lines) + §19 Token Efficiency (67 lines) + `DEC-2026-05-17-005` (hook architecture amendment to use Claude Code's actual PascalCase event taxonomy and add `.claude/git-hooks/` for git-layer enforcement) + hook directory restructure (`SessionStart`, `SessionEnd`, `PreToolUse`, `PostToolUse`, `SubagentStart`, `SubagentStop`, `FileChanged`, `ConfigChange`).
  - `3a48b0a` — §20 Agent Orchestration (100 lines: seven subagent roles + cost-aware dispatch by change tier + two-stage spec-then-quality review + LLM06 prevention strategies + MITRE ATLAS adversarial considerations) + §21 Self-Evolving Knowledge (96 lines: bounded evolution categories + confidence thresholds + LLM04 data-poisoning mitigation + four input streams).
  - Phase 2 closing commit (this one) — §22 Continual Improvement (67 lines: three improvement loops feeding each other — citation refresh, evolution proposals, telemetry analysis) + `templates/CLAUDE.md.template` (1091 lines, adopter-facing CLAUDE.md with header-level customization convention + inline ADOPTER-CUSTOMIZE markers at §7/§8/§9) + `docs/phase-2-plan.md` (Phase 2 implementation plan, committed for transparency per Decision 2 of the plan) + ROADMAP and CHANGELOG closeout.
- Phase 2 used the DEC-2026-05-17-004 research discipline throughout: authoritative source verification was performed via Claude's web tools on Anthropic infrastructure; no fetched content reached the developer filesystem except as synthesized citations and rules within markdown files. Sources verified during Phase 2: OWASP Top 10 for LLMs 2025 (LLM04, LLM06), NIST SP 800-218 v1.1 (SSDF), MITRE ATLAS v5.4.0 (February 2026), Anthropic Skill authoring best practices, Claude Code Hooks reference. Comparative-only source: Superpowers framework README (per DEC-004 Clause 6 — comparative research stays separate from authoritative citation).

### Changed

- **CLAUDE.md split into operational contract + extended architecture.** §15–§22 extracted from `CLAUDE.md` to new `docs/ARCHITECTURE.md` to keep per-session context under the 40k-char performance threshold. CLAUDE.md now holds §1–§14 (the operational contract that loads every session) and an "Extended architecture" pointer section. ARCHITECTURE.md holds the deeper architecture sections (mode scaling, empirical verification, citation discipline, hooks, token efficiency, agent orchestration, self-evolving knowledge, continual improvement). Three cross-references in §3, §4, §6 updated to point to `docs/ARCHITECTURE.md §15`. No content removed — just relocated. Result: CLAUDE.md from 75.9k → 37.8k chars (under threshold), ARCHITECTURE.md at 40.5k. Adopter `templates/CLAUDE.md.template` not yet split — deferred until adopter-distribution work in a later phase. (Commit `a630540`, 2026-05-19.)

### Phase 3 — In Progress

- `docs/phase-3-plan.md` drafted (Phase 3 implementation plan for `docs/WORKFLOW.md`). Specifies per-section mini-specs, per-section QC criteria, implementation order, sources (primarily internal synthesis of CLAUDE.md §3 + ARCHITECTURE.md §15/§18/§19/§20 + DECISIONS.md DEC-003/DEC-005), and five open decisions for Checkpoint 1 (TGF context injection mechanism for hooks, subagent schema format, hook section organization, worked-example domain choice, commit grouping). Surfaced one design question requiring resolution: how TGF injects `project_mode` and `change_tier` into Claude Code hooks (Claude Code's hook stdin JSON doesn't carry TGF concepts). Per Phase 2 Decision 2, the plan is committed for transparency before implementation. Estimated implementation effort: one focused session (~4–5 hours active work).
- **Checkpoint 1 cleared (2026-05-19).** All five decisions resolved. Decision A (TGF context injection mechanism) captured as `DEC-2026-05-19-006` (TGF session state architecture — file-based, session-keyed at `.tgf/state/sessions/{session_id}.json`); Decisions B–E (subagent schema format = TypeScript-style interface notation; hook contract organization = per-event with purpose cross-reference table; worked-example domain = generic; commit grouping = two commits split structure/content) captured in `docs/phase-3-plan.md` §7. WORKFLOW.md implementation cleared to begin next session.
- `DEC-2026-05-19-006` added to `DECISIONS.md`: TGF session state architecture. Specifies session-keyed JSON file at `.tgf/state/sessions/{session_id}.json` for `project_mode`, `change_tier`, `current_stage`, and forward-compatible future per-session state. Orchestrator writes; hooks and meta-skills read. Closes the gap left by DEC-003 Clause 2 (which invented TGF concepts into hook stdin JSON) and DEC-005 (which fixed event names but didn't address TGF-context injection). Constrains Phase 11 (orchestration meta-skill) and Phase 12 (hook scripts). Approved subject to Phase 3 Step 1 spot-check of the Claude Code Hooks reference.
- **Phase 3 Step 1 spot-check completed (2026-05-19).** Re-verified Claude Code Hooks reference. Confirmed DEC-006 is correct — no built-in mechanism exists for SessionStart→downstream-hook state sharing; file-based session-keyed state is the only documented path. Surfaced three adjacent findings: (1) richer `hookSpecificOutput.PreToolUse.permissionDecision` schema (`"allow" | "deny" | "ask" | "defer"`) provides cleaner block UX than exit 2 + stderr; (2) `CLAUDE_ENV_FILE` mechanism could complement DEC-006 by exposing TGF context to Bash tool invocations; (3) richer hook stdin JSON fields (`transcript_path`, `effort`, agent context) and additional events (`Setup`, `InstructionsLoaded`, `UserPromptExpansion`, `Elicitation`) exist beyond DEC-005's enumeration. ARCHITECTURE.md §18 remains accurate (its "at minimum" hedge holds); WORKFLOW.md §6 uses the richer schema.
- **Phase 3 WORKFLOW.md commit 1 of 2 (this commit).** Lands §1 (Purpose and Scope), §2 (Conceptual Model with stage state machine, transitions, termination conditions, handoff contract table), §4 (Subagent Output Schemas — TypeScript-style interfaces for all 7 roles per Decision B, with worked example per role per per-section QC), §5 (Tier and Mode Scaling Tables with composition rule and 3 worked composition examples), §6 (Hook Integration Contracts — "Hooks by purpose" cross-reference table per Decision C, per-event contracts for all 8 used events, TGF context injection mechanism with session_id validation hardening, richer `hookSpecificOutput` schema with `"defer"` semantics flagged for Phase 12 re-verification, three universal hooks with `WAIVER-LOG` override semantics), §9 (Reference). §3 (Per-Stage Specifications), §7 (Debugging Variant), §8 (Worked Examples) ship in commit 2.
